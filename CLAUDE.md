# Bassoon Key App

ファゴット(バスーン)の運指を記録・シェアするWebアプリ。作者はファゴット奏者で、エンジニア経験は独学レベル。説明は丁寧に、専門用語は補足しながら進めること。やり取りは日本語・敬語で。

## 構成

- 静的サイト、ビルド工程なし、依存ゼロ(Vanilla JS)。この方針は当面維持する。
- `index.html` / `app.js` / `app.css` / `picture/bassoon_key.svg`(キー配置図、30個のpath)

## 最初に読むもの

**開発計画・設計判断はすべて `docs/ROADMAP.md` にある。機能追加・改善の依頼を受けたら必ず先に読むこと。**

要点だけ書くと:

- 運指データスキーマ v1(`{version, note, keys: {キーID: 0|1|2}, label}`)が全機能の土台。0=開放, 1=押す, 2=特殊。
- Phase 0(SVGの各pathへのID付与+状態管理導入)が未完のうちは、保存・共有系の機能は実装しないこと。順序は ROADMAP の Phase 0→4 に従う。
- SVGのどのpathがどのキーかはコードから判別できない。キーIDの命名・対応付けは必ず作者に確認する(確定後は `docs/KEYMAP.md` に記録)。
- スキーマ変更時は `version` を上げ、旧形式の読み込み互換を残す。
